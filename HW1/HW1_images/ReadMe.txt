The other images required for this assignment, mri.tif and eight.tiff, are already in the default path of MATLAB. They can be loaded simply by the following function:
Img = imread('mri.tif');
Img = imread('eigth.tif');